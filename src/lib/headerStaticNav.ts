// Static (non admin-managed) header nav — shared by desktop Header and HeaderMobileMenu
// so both stay in sync.
export const RESOURCES_ITEMS = [
  { label: 'Blog', href: '/blogs' },
  { label: 'About Us', href: '/about-us' },
] as const;
